// Shapes.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>


void square_shape(int a, int b) {
    for ( a = 0 ; a < 5 ; a++){
        for (a = 0; i < b; a++) {
            cout << "* ";
        }
}

int main()
{
    int i, size;
    cout << " Enter the size of the square : ";
    cin >> size;

    square_shape(i, size);
